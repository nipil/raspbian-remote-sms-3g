# config

Ensure that you have a crontab, by using 'crontab -e' saving and exiting

Add and update parameters in "cron.sh"

Add or tune the entries you want in "crontab.txt"


# install

run INSTALL.sh (which replaces current crontab with content of "crontab.txt")
